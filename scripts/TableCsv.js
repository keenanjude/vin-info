export default class {
  /**
  *
  * @param {HTMLTableElement} root the table element which will display the csv
  */
  constructor(root) {
     this.root = root;
     console.log("I am constructed");
  }
}
